# Copy this file to .env and fill in your real values. NEVER commit .env.

# --- Secrets ---
OPENAI_API_KEY=
PINECONE_API_KEY=

# --- Pinecone ---
# The name of your existing index (the one holding the ~90 Hindi Osho books).
PINECONE_INDEX=

# --- Models (must match how the index was built) ---
# Query embeddings MUST use the same model the index was built with.
EMBEDDING_MODEL=text-embedding-3-large
CHAT_MODEL=gpt-4o-mini

# --- App behavior ---
# Which languages the app may offer. JSON array or comma-separated. Only "hi"
# has data today; add "en" once the English books are ingested.
SUPPORTED_LANGUAGES=["hi"]

# Comma-separated allowed CORS origins, or * for all (fine for local dev).
CORS_ORIGINS=*

# Per-IP rate limit for /api/wisdom (slowapi syntax).
RATE_LIMIT=20/minute

# How many chunks to retrieve per query.
TOP_K=3
